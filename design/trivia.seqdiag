participant Host
participant Player
participant socketServer
participant Trivia API
participant TriviaGameState

Host->Host:Clicks "create lobby"
Host->socketServer:Sends create-lobby request
Host<--socketServer:Returns lobby code
Host->Player:Click and drag to create a request or\ntype it in the source area to the left
Host<--Player:drag to move
note over Player,socketServer:Click Help menu for **instructions** and **examples**
Player->(3)socketServer:non-instantaneous message